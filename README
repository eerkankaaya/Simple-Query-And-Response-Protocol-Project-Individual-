The simple query and response protocol is an individually written protocol. It performs the operations "Verify directory existence, Check file existence, Determine if an existing file has been modified after a specificied timestamp, Identify files with a specificied extension that have been modified after a given timestamp". There is also a header structure divided into bits. You can access the RFCH document from the project files.
